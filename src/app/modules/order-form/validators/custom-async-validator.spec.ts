import { CustomAsyncValidator } from './custom-async-validator';

describe('CustomAsyncValidator', () => {
  it('should create an instance', () => {
    expect(new CustomAsyncValidator()).toBeTruthy();
  });
});
